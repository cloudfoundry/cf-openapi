type: object
properties:
  guid:
    type: string
  app:
    type: object
    properties:
      guid:
        type: string
      process:
        type: object
        properties:
          type:
            type: string
  weight:
    type: integer
  port:
    type: integer
  protocol:
    type: string
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
  metadata:
    $ref: './Metadata.yaml'
  links:
    $ref: './Links.yaml'
description: A route destination is a specification for where traffic on a route should be directed.

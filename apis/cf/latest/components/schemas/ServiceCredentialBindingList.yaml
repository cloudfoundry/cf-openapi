type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
  resources:
    type: array
    items:
      oneOf:
        - $ref: './AppCredentialBinding.yaml'
        - $ref: './KeyCredentialBinding.yaml'
